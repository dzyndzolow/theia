# SA-422 — rekord integracji

## Własność

- **Zadanie:** SA-422
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M4 — Zewnętrzne Sensory Feedbacku)

## Granice zadania

- **Cel z karty:** Dołączyć sensory przez wspólny contribution point (`CanFeedbackProvider`), w szczególności pomiar prądu i próg $\Delta I$ z zasilacza laboratoryjnego SCPI/serial oraz piny GPIO.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-scpi-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-gpio-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-scpi-feedback-provider.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-422.md`
- **Poza zakresem:** Kompozytory ramek (SA-418) i generatory fal (SA-419).
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Pomiary fizyczne ze złączy SCPI socket/serial lub GPIO.
- **Wyjścia i ich konsumenci:** Zdarzenia `FeedbackEvent` (`source: 'SCPI_POWER' | 'GPIO'`) do `CanExperimentEventBus`.
- **Zależności wymagające AKCEPTACJI:** SA-412, SA-410 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** Providerzy w `@theia/can-bus`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Poprawność detekcji skoku prądu $\Delta I$ i generowania zdarzenia `FEEDBACK`.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe mocka zasilacza SCPI i GPIO providera.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-scpi-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-gpio-feedback-provider.ts` (nowy)
  - `packages/can-bus/src/node/can-scpi-feedback-provider.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 147/147 passing (w tym 2 nowe testy sensorów SCPI i GPIO).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-422.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Sensory nie posiadają praw do nadawania ramek CAN (strictly NO TX).

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
