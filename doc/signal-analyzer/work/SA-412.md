# SA-412 — rekord integracji

## Własność

- **Zadanie:** SA-412
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M2 — Feedback manualny i RX-delta)

## Granice zadania

- **Cel z karty:** Rejestrować feedback manualny (`POSITIVE/NEGATIVE/UNCERTAIN`) z typem i monotonicznym timestampem oraz automatycznie wykrywać różnice RX-delta (nowe ID, zmiana częstotliwości, zmiana payloadu) względem baseline z wykluczeniem local echo.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-feedback.ts` (nowy)
  - `packages/can-bus/src/node/can-feedback-service.ts` (nowy)
  - `packages/can-bus/src/node/can-feedback-service.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-412.md`
- **Poza zakresem:** Algorytmy scoringu okna przyczynowego (SA-413) i panele UI.
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Strumień odebranych ramek RX z `CanSocketService`, manualne akcje feedbacku, `CanExperimentEventBus`.
- **Wyjścia i ich konsumenci:** Zdarzenia `FeedbackEvent` w `CanExperimentEventBus`. Konsumenci: `CanCandidateRanker` (SA-413), `CanAdaptiveIdentify` (SA-414).
- **Zależności wymagające AKCEPTACJI:** SA-411, SA-410 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** DI token `CanFeedbackService` w `can-backend-module.ts`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Sprawdzenie precyzji detekcji nowych ID i ignorowania local echo.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe manualnego i automatycznego wykrywania RX-delta.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-feedback.ts` (nowy)
  - `packages/can-bus/src/node/can-feedback-service.ts` (nowy)
  - `packages/can-bus/src/node/can-feedback-service.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 127/127 passing (w tym 2 nowe testy feedbacku i RX-delta).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-412.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Gotowe pod silnik korelacji i rankingu kandydatów w SA-413.

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
