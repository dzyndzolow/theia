# SA-414 — rekord integracji

## Własność

- **Zadanie:** SA-414
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M2 — Adaptacyjne Identify i Minimalizacja)

## Granice zadania

- **Cel z karty:** Adaptacyjne replay, izolacja kandydata (`ISOLATE_SINGLE`), testy pominięcia (`OMISSION_TEST`), próby kontrolne sham/no-TX, minimalizacja sekwencji (delta debugging) oraz bisekcja okresu keep-alive.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-adaptive-identify.ts` (nowy)
  - `packages/can-bus/src/node/can-adaptive-replay.ts` (nowy)
  - `packages/can-bus/src/node/can-adaptive-replay.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-414.md`
- **Poza zakresem:** Bit-level discovery / walking-one (SA-415) i numeric sweep (SA-416).
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Raport kandydatów z `CanCandidateRanker` (SA-413), logi z `CanRecorderService` (SA-406), `CanTransmitService`, `CanFeedbackService`.
- **Wyjścia i ich konsumenci:** Zminimalizowany wzorzec wybudzający (`MinimalWakeupRecipe`), optymalny interwał keep-alive, raport pewności identyfikacji.
- **Zależności wymagające AKCEPTACJI:** SA-413, SA-412, SA-411, SA-406, SA-410, SA-409 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** DI token `CanAdaptiveReplayService` w `can-backend-module.ts`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Weryfikacja bisekcji keep-alive i minimalizacji sekwencji na symulatorze `CanDeviceLabSimulator`.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe izolacji, pominięcia i minimalizacji sekwencji.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-adaptive-identify.ts` (nowy)
  - `packages/can-bus/src/node/can-adaptive-replay.ts` (nowy)
  - `packages/can-bus/src/node/can-adaptive-replay.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 132/132 passing (w tym 4 nowe testy minimalizacji sekwencji delta debugging, bisekcji keep-alive i serwisu replay).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-414.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Zakończono pierwszy pionowy wycinek (vertical slice) wybudzania E2E. Gotowe do kontynuacji w M3 (odkrywanie bitów/pól SA-415, SA-416).

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
