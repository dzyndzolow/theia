# SA-406 — rekord integracji

## Własność

- **Zadanie:** SA-406
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4 / 4A (Kamień milowy M1 — Niskopoziomowy nadajnik CAN i Replay)

## Granice zadania

- **Cel z karty:** Niskopoziomowe TX Classical CAN/CAN FD, logowanie i replay z gwarancją zachowania proporcjonalnych odstępów czasowych oraz egzekwowaniem polityki bezpieczeństwa SA-410.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/node/can-transmit-service.ts` (nowy)
  - `packages/can-bus/src/node/can-player-service.ts` (nowy)
  - `packages/can-bus/src/node/can-recorder-service.ts` (nowy)
  - `packages/can-bus/src/node/can-transmit-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-player-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-recorder-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-backend-module.ts` (DI bindings)
  - `packages/can-bus/src/node/index.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-406.md`
- **Poza zakresem:** Silnik wieloetapowych kampanii wybudzających (SA-411) i panele UI (SA-412+).
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Żądania nadania z sesji eksperymentu (`CanTransmitRequest`), ramki z odtwarzacza logów (`CanPlayerService`), konfiguracja sesji i `CanSafetyPolicy` (SA-410).
- **Wyjścia i ich konsumenci:** Zdarzenia `TxFrameEvent` w `CanExperimentEventBus`, ramki fizycznie wysyłane do adaptera socket/vcan, zrekonstruowany strumień ramek przy replay.
- **Zależności wymagające AKCEPTACJI:** SA-409 i SA-410 (ukończone).
- **Punkty montażu Theia / DI / RPC / worker:** DI tokens `CanTransmitService`, `CanPlayerService`, `CanRecorderService` w `can-backend-module.ts`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Sprawdzenie precyzji timera replay (kompensacja dryfu nanosekundowego) oraz natychmiastowego odcięcia przy `STOP` / bus-off.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe nadajnika, odtwarzacza i rejestratora.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/node/can-transmit-service.ts` (nowy)
  - `packages/can-bus/src/node/can-player-service.ts` (nowy)
  - `packages/can-bus/src/node/can-recorder-service.ts` (nowy)
  - `packages/can-bus/src/node/can-transmit-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-player-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-recorder-service.spec.ts` (nowy)
  - `packages/can-bus/src/node/can-backend-module.ts` (DI bindings)
  - `packages/can-bus/src/node/index.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 123/123 passing (w tym 7 nowych testów nadajnika, replay i rejestratora).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-406.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Gotowe pod silnik kampanii wybudzających w SA-411.

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
