# SA-411 — rekord integracji

## Własność

- **Zadanie:** SA-411
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M2 — Kampanie wybudzania)

## Granice zadania

- **Cel z karty:** Realizować deterministyczne kampanie wybudzające po zaobserwowanych ID, allowliście, jawnym zakresie lub profilu z małym bankiem deterministycznych payloadów (`00`, `FF`, `AA`, `55`, `7F`, rampa).
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-campaign.ts` (nowy)
  - `packages/can-bus/src/node/can-campaign-engine.ts` (nowy)
  - `packages/can-bus/src/node/can-campaign-engine.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-411.md`
- **Poza zakresem:** Agregacja feedbacku (SA-412), ranking przyczynowy (SA-413) i widżety UI.
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Konfiguracja kampanii (`CampaignPlanConfig`), baseline aktywnych ID, `CanTransmitService`.
- **Wyjścia i ich konsumenci:** Zlecenia prób wybudzających `CampaignTrialConfig`, zdarzenia postępu `CampaignProgress`. Konsumenci: `CanFeedbackService` (SA-412), `CanCandidateRanker` (SA-413).
- **Zależności wymagające AKCEPTACJI:** SA-406, SA-409, SA-410 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** DI token `CanCampaignEngine` w `can-backend-module.ts`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Determinizm generowania planu prób i kolejności wysyłek.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe generowania planu kampanii i sekwencyjnego wykonania prób.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-campaign.ts` (nowy)
  - `packages/can-bus/src/node/can-campaign-engine.ts` (nowy)
  - `packages/can-bus/src/node/can-campaign-engine.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 125/125 passing (w tym 2 nowe testy planera i silnika kampanii).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-411.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Gotowe pod integrację zbierania feedbacku w SA-412.

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
