# SA-420 — rekord integracji

## Własność

- **Zadanie:** SA-420
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M5 — Izolowany Runtime Python)

## Granice zadania

- **Cel z karty:** Uruchamiać skrypty w osobnym procesie z capability API (`subscribe/send/schedule/variables/feedback/annotate/stop`), ścisłą walidacją `CanSafetyPolicy`, strażnikiem heartbeat i kontrolowanym hot-reloadem.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-script-protocol.ts` (nowy)
  - `packages/can-bus/src/node/can-python-runner.ts` (nowy)
  - `packages/can-bus/src/node/can-python-runner.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-420.md`
- **Poza zakresem:** Archiwizacja eksperymentu i raporty (SA-424).
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Kod skryptu Python (`scriptContent`), konfiguracja sesji, `CanTransmitService`, `CanExperimentEventBus`.
- **Wyjścia i ich konsumenci:** Żądania nadania ramek przekazywane do `CanTransmitService`, zdarzenia feedbacku, aktualizacje zmiennych globalnych.
- **Zależności wymagające AKCEPTACJI:** SA-419, SA-418, SA-410 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** DI token `CanPythonRunner` w `can-backend-module.ts`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Sprawdzenie reakcji na brak heartbeat, próbę wysłania poza allowlistę oraz procedurę hot-reload.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe protokołu IPC capability API i zarządcy cyklu życia procesu.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-script-protocol.ts` (nowy)
  - `packages/can-bus/src/node/can-python-runner.ts` (nowy)
  - `packages/can-bus/src/node/can-python-runner.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
  - `packages/can-bus/src/node/index.ts`
  - `packages/can-bus/src/node/can-backend-module.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 151/151 passing (w tym 4 nowe testy protokołu IPC, wysyłki ramek, zmiennych, feedbacku i zatrzymania).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-420.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Skrypt nie posiada bezpośredniego dostępu do sprzętu i musi komunikować się przez capability API. Gotowe pod archiwizację eksperymentu (SA-424).

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
