# SA-006 — rekord integracji

## Własność

- **Zadanie:** SA-006
- **Wykonawca/model:** Antigravity (Gemini 3.6 Flash)
- **Status:** ZAAKCEPTOWANE
- **Data rezerwacji:** 2026-08-05 (poprawka Q-02)
- **Karta zadania i faza:** Faza 0 — SA-006, CSS widgetu + wykres FPS

## Granice zadania

- **Cel z karty:** Stylizacja CSS widgetu z wykorzystaniem zmiennych motywu Theia oraz rysowanie płynnego wykresu FPS / Bus Load w czasie rzeczywistym na surowym Canvas2D z obsługą High-DPI (Retina) i dynamicznymi kolorami motywu.
- **Zmiany w kodzie:** `can-widget.css`, `fps-canvas.ts` (`setColors`), `can-widget.ts` (`drawChart` z `getComputedStyle`), `fps-canvas.spec.ts`.
- **Zmiany w dokumentacji:** Ten rekord, `SIGNAL-ANALYZER-TASKS.md`, `doc/signal-analyzer/execution.md`.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Statystyki `CanStatistics` z `CanWidget`.
- **Wyjścia i ich konsumenci:** Wycinek elementu `<canvas>` w widżecie Lumino.
- **Zależności wymagające AKCEPTACJI:** SA-004, SA-005.

## Plan weryfikacji

- **Testy i komendy:** `npx lerna run compile/lint/test --scope @theia/can-bus`.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:** `packages/can-bus/src/browser/style/can-widget.css`, `packages/can-bus/src/browser/fps-canvas.ts`, `packages/can-bus/src/browser/can-widget.ts`, `packages/can-bus/src/browser/fps-canvas.spec.ts`.
- **Wyniki weryfikacji:** `npx lerna run compile/lint/test --scope @theia/can-bus` (OK, 23/23 passing).

## Decyzja supervisora

- **Decyzja:** AKCEPTACJA
- **Uzasadnienie:** Zastąpiono statyczne kolory w Canvas2D dynamiczną paletą pobieraną z motywu Theia przez `getComputedStyle(this.node)` (`fpsColor`, `busLoadColor`, `gridColor`) (Q-02).
- **Warunki przekazania do następnego zadania:** SA-007 i SA-008 mogą być wykonywane.
