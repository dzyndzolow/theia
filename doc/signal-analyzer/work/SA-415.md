# SA-415 — rekord integracji

## Własność

- **Zadanie:** SA-415
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M3 — Odkrywanie opcji binarnych)

## Granice zadania

- **Cel z karty:** Mutować pojedynczy bit względem znanego baseline, walking-one, walking-zero, maski bitów stałych i badanych, mapowanie bit -> kontrolka/feedback z confidence.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-bit-discovery.ts` (nowy)
  - `packages/can-bus/src/common/can-bit-discovery.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-415.md`
- **Poza zakresem:** Odkrywanie pól liczbowych i tekstu ISO-TP (SA-416) oraz panele UI.
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Ramka bazowa (`CanFrame`), maska badanych bitów (`bitMask`), `CanTransmitService`, `CanFeedbackService`.
- **Wyjścia i ich konsumenci:** `BitDiscoveryReport` z mapą `DiscoveredBitOption` (byteIndex, bitIndex, activeState, feedbackType, confidence).
- **Zależności wymagające AKCEPTACJI:** SA-414, SA-413, SA-412, SA-411, SA-410, SA-409 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** Moduł algorytmów i analityki bitowej w `@theia/can-bus`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Poprawność detekcji bitów walking-one i walking-zero na symulatorze DUT z kontrolkami.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe generowania mutacji bitowych i korelacji z feedbackiem.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-bit-discovery.ts` (nowy)
  - `packages/can-bus/src/common/can-bit-discovery.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 138/138 passing (w tym 2 nowe testy mutacji bitów walking-one i mapowania kontrolek).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-415.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Gotowe do połączenia z odkrywaniem pól liczbowych i generatorami.

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
