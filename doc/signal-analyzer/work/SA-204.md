# SA-204 — rekord integracji

## Własność

- **Zadanie:** SA-204
- **Wykonawca/model:** Antigravity (Gemini 3.6 Flash)
- **Status:** ZAAKCEPTOWANE
- **Data rezerwacji:** 2026-08-05
- **Karta zadania i faza:** Faza 2 — SA-204, Dekoder CAN jako `DecoderProvider`

## Granice zadania

- **Cel z karty:** Implementacja dekodera `CanDecoderProvider` w `@theia/can-bus` realizującego kontrakt `DecoderProvider` z `@theia/signal-core`, przetwarzającego surowe próbki sygnałowe lub ramki binarne na adnotacje `ProtocolAnnotation` poprzez strumieniowy generator asynchroniczny z małą alokacją pamięci i operacjami DataView/BigInt na payloadzie.
- **Zmiany w kodzie:** `packages/can-bus/src/common/can-decoder-provider.ts`, `packages/can-bus/src/common/index.ts`, `packages/can-bus/src/common/can-decoder-provider.spec.ts`.
- **Zmiany w dokumentacji:** Ten rekord, `SIGNAL-ANALYZER-TASKS.md`, `doc/signal-analyzer/execution.md`, `doc/signal-analyzer/work/README.md`.
- **Poza zakresem:** Dekoder UART (SA-205) i dokumentacja (SA-206).
- **Pliki planowane:** `packages/can-bus/src/common/can-decoder-provider.ts`, `packages/can-bus/src/common/index.ts`, `packages/can-bus/src/common/can-decoder-provider.spec.ts`, `SIGNAL-ANALYZER-TASKS.md`, `doc/signal-analyzer/work/SA-204.md`, `doc/signal-analyzer/execution.md`, `SIGNAL-ANALYZER-CHANGELOG.md`.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Surowe pakiety / `SampleWindow` lub strumień `AsyncIterable<ProtocolAnnotation>` z powłoki `signal-core`.
- **Wyjścia i ich konsumenci:** AsyncIterable<ProtocolAnnotation> z typem `annotation:can`.
- **Zależności wymagające AKCEPTACJI:** SA-101..SA-203 (ZAAKCEPTOWANE).
- **Punkty montażu Theia / DI / RPC / worker:** System dekodowania CAN w `can-bus`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Zgodność z zamrożonym `DecoderProvider` (`apiVersion: 1.0.0`).
- **Ryzyka współdzielenia plików lub konfliktów:** `packages/can-bus/src/common/index.ts`.

## Plan weryfikacji

- **Testy i komendy:** `npx lerna run compile --scope @theia/can-bus`; `npx lerna run test --scope @theia/can-bus`.
- **Benchmark lub pomiar, jeśli wymagany:** Test przetwarzania binarnego DataView/BigInt dla ramek CAN z poprawną ekstrakcją ID, DLC, IDE i RTR.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Generator asynchroniczny z opcją pauzy i pobieraniem adnotacji.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:** `packages/can-bus/src/common/can-decoder-provider.ts`, `packages/can-bus/src/common/index.ts`, `packages/can-bus/src/common/can-decoder-provider.spec.ts`, `SIGNAL-ANALYZER-TASKS.md`, `SIGNAL-ANALYZER-CHANGELOG.md`, `doc/signal-analyzer/work/SA-204.md`, `doc/signal-analyzer/work/README.md`, `doc/signal-analyzer/execution.md`.
- **Wyniki weryfikacji:** `npx lerna run compile --scope @theia/can-bus` (OK); `npx lerna run test --scope @theia/can-bus` (26/26 passing, 96.69% overall coverage).
- **Zaktualizowana dokumentacja funkcjonalna:** Nie dotyczy na tym etapie.
- **Changelog:** wpis `2026-08-05 | Antigravity (Gemini 3.6 Flash) | SA-204 | STATUS: DONE`.
- **Znane ograniczenia / zadania następcze:** SA-205 stworzy dekoder UART jako 2. protokół.

## Decyzja supervisora

- **Decyzja:** AKCEPTACJA
- **Uzasadnienie:** Dekoder `CanDecoderProvider` poprawnie przetwarza próbki surowe i ramki binarne ze zgodnością dla `DecoderProvider` (`apiVersion: 1.0.0`), generując strumieniowe adnotacje protokołu z użyciem operacji BigInt/DataView i niską alokacją pamięci. Testy jednostkowe (26/26 passing, 96.69% coverage) zakończone pomyślnie.
- **Warunki przekazania do następnego zadania:** SA-205 może utworzyć dekoder UART jako 2. protokół.
