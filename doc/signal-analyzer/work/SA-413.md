# SA-413 — rekord integracji

## Własność

- **Zadanie:** SA-413
- **Wykonawca/model:** Antigravity (Gemini 3.7 Flash)
- **Status:** GOTOWE DO REWIZJI
- **Data rezerwacji:** 2026-08-28
- **Karta zadania i faza:** Faza 4A — CAN Device Lab (Kamień milowy M2 — Ranking przyczynowy)

## Granice zadania

- **Cel z karty:** Budować ranking kandydatów i historię dowodów na podstawie korelacji czasowej zdarzeń `FEEDBACK` z wysłanymi ramkami `TX_FRAME` w oknie przyczynowym z uwzględnieniem opóźnienia operatora / automatycznego RX-delta.
- **Zmiany w kodzie:**
  - `packages/can-bus/src/common/can-candidate-ranker.ts` (nowy)
  - `packages/can-bus/src/common/can-candidate-ranker.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
- **Zmiany w dokumentacji:** `doc/signal-analyzer/work/SA-413.md`
- **Poza zakresem:** Adaptacyjne replay i minimalizacja sekwencji (SA-414).
- **Pliki planowane:** Wskazane w zmianach w kodzie.

## Kontrakt integracji

- **Wejścia i ich właściciele:** Dziennik zdarzeń `ExperimentJournal` (zawierający `TxFrameEvent` i `FeedbackEvent`).
- **Wyjścia i ich konsumenci:** `CandidateRankingReport` z posortowaną listą `CandidateScore`. Konsumenci: `CanAdaptiveIdentify` (SA-414).
- **Zależności wymagające AKCEPTACJI:** SA-412, SA-411, SA-410 (spełnione).
- **Punkty montażu Theia / DI / RPC / worker:** Klasa narzędziowa i serwis analityczny w `@theia/can-bus`.
- **Wpływ na zamrożone kontrakty i `apiVersion`:** Brak wpływu na `@theia/signal-core`.
- **Ryzyka współdzielenia plików lub konfliktów:** Brak.

## Plan weryfikacji

- **Testy i komendy:** `yarn --cwd packages/can-bus test`, `yarn --cwd packages/can-bus lint`, `npx lerna run compile --scope @theia/can-bus`
- **Benchmark lub pomiar, jeśli wymagany:** Determinizm obliczeń scoringu przy zmiennych oknach reakcji.
- **Ścieżka integracyjna do ręcznego sprawdzenia:** Testy jednostkowe łączenia zdarzeń w oknie przyczynowym.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:**
  - `packages/can-bus/src/common/can-candidate-ranker.ts` (nowy)
  - `packages/can-bus/src/common/can-candidate-ranker.spec.ts` (nowy)
  - `packages/can-bus/src/common/index.ts`
- **Wyniki weryfikacji:** Kompilacja 0 błędów, lint 0 błędów, testy 128/128 passing (w tym nowy test rankera przyczynowego).
- **Zaktualizowana dokumentacja funkcjonalna:** `doc/signal-analyzer/work/SA-413.md`
- **Changelog:** wpis w `SIGNAL-ANALYZER-CHANGELOG.md` z 2026-08-28
- **Znane ograniczenia / zadania następcze:** Gotowe pod silnik adaptacyjnego replayu i minimalizacji sekwencji w SA-414.

## Decyzja supervisora

- **Decyzja:** oczekuje
- **Uzasadnienie:**
- **Warunki przekazania do następnego zadania:**
