# SA-003 — rekord integracji

## Własność

- **Zadanie:** SA-003
- **Wykonawca/model:** GitHub Copilot / Antigravity
- **Status:** ZAAKCEPTOWANE
- **Data rezerwacji:** 2026-08-05 (poprawki C-01 / Q-03)
- **Karta zadania i faza:** Faza 0 — SA-003, Backend: can-rpc-service.ts

## Granice zadania

- **Cel z karty:** Dwukierunkowa komunikacja RPC i Eventy oraz tworzenie WebSocket connection proxy w przeglądarce.
- **Zmiany w kodzie:** `can-rpc-service.ts` (`CanRpcServiceImpl`), `can-frontend-module.ts` (`WebSocketConnectionProvider.createProxy<CanRpc>` z rejestracją klienta `onBinaryFrames`), `can-widget.ts` (wywołania `startCapture()`/`stopCapture()`), `can-rpc-service.spec.ts`.
- **Zmiany w dokumentacji:** Ten rekord, `SIGNAL-ANALYZER-TASKS.md`, `doc/signal-analyzer/execution.md`.

## Kontrakt integracji

- **Wejścia i ich właściciele:** `CanSocketService` z SA-002; client RPC z `can-frontend-module.ts`.
- **Wyjścia i ich konsumenci:** `CanRpc` proxy w `CanWidget`.
- **Zależności wymagające AKCEPTACJI:** SA-001, SA-002.

## Plan weryfikacji

- **Testy i komendy:** `npx lerna run compile/lint/test --scope @theia/can-bus`.

## Przekazanie i wynik

- **Faktycznie zmienione pliki:** `packages/can-bus/src/node/can-rpc-service.ts`, `packages/can-bus/src/browser/can-frontend-module.ts`, `packages/can-bus/src/browser/can-widget.ts`, `packages/can-bus/src/node/can-rpc-service.spec.ts`.
- **Wyniki weryfikacji:** `npx lerna run compile/lint/test --scope @theia/can-bus` (OK, 23/23 passing).

## Decyzja supervisora

- **Decyzja:** AKCEPTACJA
- **Uzasadnienie:** Zaimplementowano tworzenie WebSocket connection proxy w `can-frontend-module.ts` przez `WebSocketConnectionProvider.createProxy<CanRpc>` podłączając klienta callbacków `onBinaryFrames` do `CanWidget.addBinaryChunk()` oraz komendy w widget (C-01, Q-03).
- **Warunki przekazania do następnego zadania:** SA-007 i SA-008 mogą być wykonywane.
